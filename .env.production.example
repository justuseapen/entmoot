# Entmoot Production Environment Variables
# Copy this file to .env and fill in the values

# ===========================================
# DOMAIN CONFIGURATION
# ===========================================
# Your production domain (without https://)
DOMAIN=your-domain.com

# CORS allowed origins (include https://)
# For single origin: https://your-domain.com
# For multiple origins (comma-separated): https://your-domain.com,https://app.your-domain.com
# IMPORTANT: Must exactly match the Origin header sent by browsers
# Common issues:
# - Missing https:// prefix
# - Trailing slash (don't include)
# - www vs non-www mismatch
CORS_ORIGINS=https://your-domain.com

# ===========================================
# DATABASE (PostgreSQL)
# ===========================================
POSTGRES_USER=entmoot
POSTGRES_PASSWORD=CHANGE_ME_TO_SECURE_PASSWORD
POSTGRES_DB=entmoot_production

# ===========================================
# RAILS APPLICATION
# ===========================================
# Get this from backend/config/master.key
RAILS_MASTER_KEY=

# Generate with: docker run --rm ruby:3.3 ruby -e "require 'securerandom'; puts SecureRandom.hex(64)"
SECRET_KEY_BASE=

# ===========================================
# AI FEATURES (Anthropic Claude)
# ===========================================
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=

# ===========================================
# EMAIL (Resend)
# ===========================================
# Sign up at: https://resend.com/
# Free tier: 100 emails/day, 3,000/month
# Create an API key with sending permissions
# IMPORTANT: You must verify your domain at https://resend.com/domains
RESEND_API_KEY=

# The "from" address for outgoing emails
# Must match a verified domain in Resend
# Current addresses used: hello@entmoot.app, reminders@entmoot.app
MAILER_FROM=hello@your-domain.com

# ===========================================
# ERROR TRACKING (AppSignal)
# ===========================================
# Sign up at: https://appsignal.com/
# Get your Push API Key from: App Settings > Push & Deploy > Push Key
APPSIGNAL_PUSH_API_KEY=
APPSIGNAL_ACTIVE=true

# ===========================================
# OPTIONAL SETTINGS
# ===========================================
# Sidekiq worker concurrency (default: 5)
# SIDEKIQ_CONCURRENCY=5

# ===========================================
# DO NOT CHANGE (internal service URLs)
# ===========================================
# These are set automatically by Docker Compose
# REDIS_URL=redis://redis:6379/0
# DATABASE_URL=postgres://...
